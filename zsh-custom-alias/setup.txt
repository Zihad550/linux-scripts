Add it to .zshrc file
Steps:
1. create a zsh directory
2. create aliasrc file and copy contents of aliasrc

# Load aliases and shortcuts if existent.
[ -f "$HOME/zsh/aliasrc" ] && source "$HOME/zsh/aliasrc"